node_modules/
dist/
build/
.next/
out/
*.log
.DS_Store
.env
.env.local
.vite/
# Registry auth tokens for the private @gocharting/chart-sdk package — never commit
.npmrc

# Licensed SDK bundle — copy in locally, never commit
index.umd.js

# Python (Django example)
.venv/
__pycache__/
*.pyc

# Android (machine-specific SDK path / build output)
local.properties
.gradle/
build/

# Lockfiles — examples pin the SDK via package.json, no lockfiles tracked
package-lock.json
pnpm-lock.yaml
yarn.lock

# Flutter — generated by `flutter create .`; the example ships only
# lib/, assets/ and pubspec.yaml (see flutter/README.md)
flutter/.metadata
flutter/.gitignore
flutter/analysis_options.yaml
flutter/pubspec.lock
flutter/android/
flutter/ios/
flutter/linux/
flutter/macos/
flutter/web/
flutter/windows/
flutter/test/
